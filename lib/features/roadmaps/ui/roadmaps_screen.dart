import 'package:flutter/material.dart';

class RoadMapsScreen extends StatelessWidget {
  const RoadMapsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(
        child: Text("Welcome to RoadMaps Screen "),
      ),
    );
  }
}
